/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['commsaur.mypinata.cloud', 'd124myd65w4vuo.cloudfront.net'],
    },
};

module.exports = nextConfig;
